#ifndef TYPEDEFS_H
#define TYPEDEFS_H


#include <iostream>

const int N = 3;

enum class CellType {
    blank=1,
    circle=2,
    cross=3,
};
enum class Turn{
    circle=1,
    cross=2
};

#endif // TYPEDEFS_H
